{
  "name": "3d-map-generator",
  "version": "0.1.0",
  "description": "Procedural ruined city map generator for Mordheim / Tabletop Simulator",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "generate": "node src/index.js",
    "preview": "node src/index.js --preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "check:file-sizes": "node scripts/checks/check-file-sizes.js",
    "check:all": "node scripts/checks/run-all.js",
    "check:all:summary": "node scripts/checks/run-all.js --summary",
    "check:all:definitions": "node scripts/checks/run-all.js --definitions",
    "check:all:report": "node scripts/checks/run-all.js --output"
  },
  "keywords": [
    "mordheim",
    "tabletop",
    "terrain",
    "procedural-generation"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pngjs": "^7.0.0",
    "three": "^0.183.2"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.2",
    "vitest": "^4.1.2"
  }
}
